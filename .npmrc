# This package is public and its lockfile must resolve to the public registry.
# Without this, a machine configured for a private feed bakes that feed's URLs
# into package-lock.json, and CI — which has no credentials for it — fails
# `npm ci` with E401.
registry=https://registry.npmjs.org/
